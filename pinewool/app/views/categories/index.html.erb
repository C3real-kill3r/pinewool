<h2><%=@categories.length%> Categories</h2>

<ol>
    <% @categories.each do |category|%>
    <li>
        <%= category.title%>
        (<%= category.products_count%> products)
    </li>
    <%end%>
</ol>